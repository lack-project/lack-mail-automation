# Project Instructions

## Examples

Examples that demonstrate normal application usage must use attributed functions with `#[OnFolderAutomation(...)]` and register them through `MailAutomation::addRules()`. The programmatic `onFolder(...)->addAutomation(...)` API may only be used in an example when that example explicitly exists to demonstrate or explain the programmatic registration API itself.
